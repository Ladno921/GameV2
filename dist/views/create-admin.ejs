<%- include('particles/header.ejs') %>

  <%- include('particles/navigation.ejs') %>

    <% if(admin) {%>
      <div class="content">
        <form class="form__center" action="/create-admin" method="post">

          <div class="form">

            <ul class="tab-group">
              <li class="tab active"><a href="">Создание аккаунта администратора</a></li>
            </ul>
            <div class="field-wrap">
              <label class="label">
                Имя
              </label>
              <input class="form-group__input" id="name" name="name" type="text" placeholder="Введите Имя" required>
            </div>

            <div class="field-wrap">
              <label class="label">
                Email Адрес
              </label>
              <input class="form-group__input" type="email" id="email" name="email" placeholder="Введите email"
                required>
            </div>

            <div class="field-wrap">
              <label class="label">
                Пароль
              </label>
              <input class="form-group__input" id="password" name="password" type="password"
                placeholder="Введите Пароль" required>
            </div>

            <button class="button1 button-block">Создать</button>

          </div>

      </div>

      </form>
      </div>
      <% } else { %>
        <h1 class="Error"> ERROR 404 </h1>
        <h1 class="Error"> Эта страница недоступна </h1>
        <% } %>

          <%- include('./particles/footer.ejs') %>